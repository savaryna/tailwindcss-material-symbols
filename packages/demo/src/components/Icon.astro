---
type Props = {
  name: string;
  class?: string;
};

const { name, class: className } = Astro.props;
---

<span
  class:list={[
    'icon dark:not-[.icon-normal,.icon-emphasis]:icon-dark',
    className,
  ]}>{name}</span
>
